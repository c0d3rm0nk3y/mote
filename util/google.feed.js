var   q = require('../node_modules/q');
var ass = require('./ass');

exports.search = function(keywords) {
  var d = q.defer();
  try  {
    
      
   } catch(ex) { d.reject(ex); }
   return d.promise;
};